import generateUtilityClass from '@mui/utils/generateUtilityClass';
import composeClasses from '@mui/utils/composeClasses';
import generateUtilityClasses from '@mui/utils/generateUtilityClasses';
export function getBarUtilityClass(slot) {
  return generateUtilityClass('MuiBar', slot);
}
export const barClasses = generateUtilityClasses('MuiBar', ['root', 'series', 'seriesLabels', 'element', 'label', 'labelAnimate']);
export const useUtilityClasses = options => {
  const {
    skipAnimation,
    classes
  } = options ?? {};
  const slots = {
    root: ['root'],
    series: ['series'],
    seriesLabels: ['seriesLabels'],
    element: ['element'],
    label: ['label', !skipAnimation && 'labelAnimate']
  };
  return composeClasses(slots, getBarUtilityClass, classes);
};